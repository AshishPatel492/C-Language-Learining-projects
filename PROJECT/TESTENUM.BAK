#include<conio.h>
#include<stdio.h>
enum boolean
{
 true,false
};
int isEven(int x)//enum boolean is Even(x)
{
 if(x%2==0)
  return(true);//1
 else
  return(false);//0
}
void main()
{
 int n;
 enum boolean result;
 clrscr();
 printf("Enter a Number:");
 scanf("%d",&n);
 result=isEven(n);
  if(result==true)
   printf("Even Number");
  else
   printf("odd Number");
 getch();
}