#include<stdio.h>
#include<conio.h>
void main()
{
 int n,N,s,i,sum=0;
 clrscr();
 printf("Enter Natural Number:\n");
 scanf("%d",&N);
 for(n=1;n<=N;n++)
  {
   s=0;
   for(i=1;i<=n;i++)
    s=s+i;
    sum=sum+s;
  }
  printf("Sum is %d",sum);
  getch();
}