#include<stdio.h>
#include<conio.h>
void main()
{
 char str[20];
 FILE *fp;
 clrscr();
 fp=fopen("h3.txt","w");//try "a"
 printf("Enter Your Name:");
 gets(str);
 fputs(str,fp);
 fclose(fp);
 getch();
}